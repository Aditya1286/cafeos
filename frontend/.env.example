# MSG91 OTP Widget — get these from the Widget section of the MSG91 dashboard
# (NOT the API/Auth Key section — that key must never go in frontend code).
VITE_MSG91_WIDGET_ID=
VITE_MSG91_WIDGET_TOKEN=
