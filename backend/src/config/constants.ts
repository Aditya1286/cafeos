// Single source of truth for the platform's brand name. Change these two
// values to rebrand the app everywhere they're referenced instead of
// grep-and-replacing across the codebase.
export const APP_NAME = 'Caos';
export const APP_SLUG = 'Caos';
